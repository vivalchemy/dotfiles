
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

# autogenerated = 1 # remove this line to remove the warning

# See https://wiki.hyprland.org/Configuring/Monitors/
# monitor=,preferred,auto,auto
monitor = ,preferred, auto, auto, mirror, eDP-1

$terminal = ghostty
$fileManager = thunar 
$menu = rofi 
$browser = zen-browser
$notesManager = obsidian
# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
exec-once = hyprctl dispatch workspace 3
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = hyprpaper
exec-once = mako
exec-once = [workspace 2 silent] $browser 
# exec-once = [workspace 3 silent] $terminal --class AlacrittyFullScreen
exec-once = [workspace 3 silent] $terminal --class=com.ghostty.fullscreen
# exec-once = [workspace 8 silent] $notesManager
# exec-once = [workspace 10 silent] $fileManager
exec-once = cliphist wipe 
exec-once = wl-paste --watch cliphist store
exec-once = $HOME/.local/bin/hyprhaven -r 

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf
source = ./catppuccin-mocha.conf # catpuccin mocha color scheme

# Set programs that you use

# Some default env vars.
# env = XCURSOR_SIZE,24
# env = QT_QPA_PLATFORMTHEME,qt5ct
# env = GTK_THEME,Adwaita:dark
# env = GTK2_RC_FILES,/usr/share/themes/Adwaita-dark/gtk-2.0/gtkrc
# env = QT_STYLE_OVERRIDE,adwaita-dark
# env = ZDOTDIR, 
source = ./env_var.conf

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    touchpad {
        natural_scroll = yes 
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    # Border and layout settings for a clean, modern look
    border_size = 0
    resize_on_border = true
    extend_border_grab_area = 10
    hover_icon_on_border = true

    # Soft, subtle gaps for breathing room
    gaps_in = 0
    gaps_out = 0
    gaps_workspaces = 0

    # Glassmorphic border colors with soft gradients
    col.inactive_border = rgba(ffffff22)
    col.active_border = rgba(ffffff44) rgba(ffffff22) 45deg

    # Layout and focus improvements
    layout = dwindle
    no_focus_fallback = false
    allow_tearing = false
}

decoration {
    # Enhanced rounding for soft, modern edges
    rounding = 0
    
    # Opacity management for focus
    active_opacity = 1.0
    inactive_opacity = 0.9
    fullscreen_opacity = 1.0
    
    # Subtle dimming for focus
    dim_inactive = true
    dim_strength = 0.1
    
    # Blur settings for glassmorphic effect
    blur {
        enabled = true
        size = 6
        passes = 3
        new_optimizations = true
        ignore_opacity = true
        
        # Subtle noise and vibrance for depth
        noise = 0.05
        contrast = 1.0
        brightness = 0.9
        vibrancy = 0.2
        vibrancy_darkness = 0.1
        
        # Blur popups for consistent look
        popups = true
        popups_ignorealpha = 0.1
    }
    
    # Soft, subtle shadows
    shadow {
        enabled = true
        range = 6
        render_power = 3
        color = rgba(00000030)
        scale = 0.95
    }
}

animations {
    enabled = true
    first_launch_animation = true
    
    # Smooth, natural bezier curves
    bezier = smoothGlass, 0.16, 1, 0.3, 1
    bezier = gentleSpring, 0.34, 1.56, 0.64, 1
    
    # Refined window animations
    animation = windows, 1, 7, smoothGlass, popin 80%
    animation = windowsOut, 1, 7, smoothGlass, popin 80%
    animation = windowsMove, 1, 7, gentleSpring
    
    # Soft transitions
    animation = border, 1, 10, smoothGlass
    animation = borderangle, 1, 8, default
    animation = fade, 1, 5, smoothGlass
    
    # Workspace transitions with subtle movement
    animation = workspaces, 1, 6, smoothGlass, slidefade 20%
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_status = master 
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = on

}

misc {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    force_default_wallpaper = -1 # Set to 0 to disable the anime mascot wallpapers
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


# Example windowrule v1
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# windowrule = maximize, ^(Alacritty)$
# windowrule = maximize, .*Vivaldi

# Assigning workspaces to applications
# windowrule = workspace 2, .*(Vivaldi).*
# windowrule = workspace 4, .*(Alacritty).*
# windowrule = workspace 6, .*(Obsidian).*
# windowrule = workspace 8, .*(Thunar).*

# Example windowrule
windowrule = float,class:^(Alacritty)$,title:^(Alacritty)$
windowrule = tile,class:^(AlacrittyFullScreen)$,title:^(Alacritty)$
windowrule = tile,class:^(com\.ghostty\.fullscreen)$
windowrule = float,class:^(com\.mitchellh\.ghostty|com\.ghostty\.transparent)$
windowrule = opacity 0.6,class:^(com\.ghostty\.transparent)$
windowrule = suppressevent maximize, class:.* # You'll probably like this.
windowrule = opacity 0.9 0.9 , class:(thunar)
windowrule = opacity 0.9 0.9 , class:(obsidian)
windowrule = float, title:^(Picture-in-Picture)$
windowrule = pin, title:^(Picture-in-Picture)$ 
windowrule = size 650 368, title:^(Picture-in-Picture)$
windowrule = move 100%-w-16 100%-w-16, title:^(Picture-in-Picture)$ 
windowrule = noinitialfocus, title:^(Picture-in-Picture)$ 

windowrule = float, initialClass:^(showmethekey-gtk)$ 
windowrule = pin, initialClass:^(showmethekey-gtk)$ 
windowrule = size 330 180, initialClass:^(showmethekey-gtk)$
windowrule = move 100%-w-16 65%-w, initialClass:^(showmethekey-gtk)$ 
windowrule = noinitialfocus, initialClass:^(showmethekey-gtk)$ 
# windowrulev2 = opacity 0.9 0 , title:.*(— Zen Browser)$ # can't do it since i like to create website can do localhost though as a fix
# windowrulev2 = maximize, workspace:current, onworkspace:current, num_windows:>=2
# windowrulev2 = opacity 0.94 override 0.93 override , class:(codium-url-handler)
# opacity acitve_opacity inactive_opacity

source = ./keymaps.conf
